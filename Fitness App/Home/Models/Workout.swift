import SwiftUI

struct Workout: Identifiable {
    let id: Int
    let title: String
    let image: String
    let tintColour: Color
    let duration: String
    let date: String
    let calories: String
}
